# Sidebar icon microanimations

Hover-based SVG microanimation experiments for the first sidebar icon set.

## Preview

Open `index.html` in a browser. The demo is self-contained and does not require a build step or external dependencies.

## Interaction

Move the pointer over a sidebar row to play its animation once. Keyboard focus triggers the same animation.
